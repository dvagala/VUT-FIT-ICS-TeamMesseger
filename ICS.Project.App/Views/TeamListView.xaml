<local:UserControlCustom x:Class="ICS.Project.App.Views.TeamListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ICS.Project.App.Views"
             xmlns:viewModels="clr-namespace:ICS.Project.App.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:TeamsListViewModel}"
             DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=TeamsListViewModel}">

    <StackPanel Grid.Row="1" Orientation="Vertical">

        <ListBox ItemsSource="{Binding Teams }">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
            <TextBox Text="{Binding NewTeam.Name, UpdateSourceTrigger=PropertyChanged}" Height="23" Margin="0,0,5,0" TextWrapping="Wrap" Width="80" />
            <Button Content="+" Command="{Binding NewTeamAddedCommand}" Width="30"/>
        </StackPanel>
    </StackPanel>
</local:UserControlCustom>
